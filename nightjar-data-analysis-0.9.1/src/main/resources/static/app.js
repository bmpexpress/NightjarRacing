"use strict";
const $=id=>document.getElementById(id),NJ={state:null,data:null,rows:[],active:"overview"},ORANGE="#f28c28",PANEL="#102435";
async function api(url,opt={}){const r=await fetch(url,{credentials:"same-origin",...opt}),type=r.headers.get("content-type")||"",b=type.includes("json")?await r.json():r;if(!r.ok)throw new Error(b?.error||r.statusText);return b}
const post=(url,body)=>api(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c])),numeric=v=>typeof v==="number"&&Number.isFinite(v),selected=e=>[...e.selectedOptions].map(o=>o.value),layout=t=>({title:t,paper_bgcolor:PANEL,plot_bgcolor:PANEL,font:{color:"#f5f7fa"},height:600,legend:{orientation:"h"}});
function request(){return{events:selected($("events")),fromDate:$("from").value||null,toDate:$("to").value||null,startTime:$("start").value,endTime:$("end").value,trimByTime:$("trim").checked,includedVariables:NJ.state?.includedVariables||[]}}
function msg(v){$("messages").innerHTML=v?`<div class="notice">${esc(v)}</div>`:""}
function show(logout){$("login").classList.add("hidden");$("app").classList.remove("hidden");$("logout").classList.toggle("hidden",!logout)}
async function boot(){const a=await api("/api/auth");if(a.required&&!a.authenticated){$("login").classList.remove("hidden");return}show(a.required);NJ.state=await api("/api/state");configure();if(NJ.state.loaded)await refresh()}
function fill(id,values,want){const e=$(id),old=e.value;e.innerHTML=values.map(v=>`<option value="${esc(v)}">${v==="__TIME__"?"Time":esc(v)}</option>`).join("");e.value=values.includes(old)?old:(values.includes(want)?want:values[0]||"")}
function configure(){const s=NJ.state;$("status").textContent=s.loadStatus||"No log loaded";msg((s.loadWarnings||[]).join(" "));$("downsample").checked=!!s.plotDownsamplingEnabled;$("maxPoints").value=s.maxPlotPoints||10000;$("maxPoints").disabled=!$("downsample").checked;$("events").innerHTML=(s.eventList||[]).map(e=>`<option value="${esc(e.event)}">${esc(e.date)} · ${esc(e.event)}</option>`).join("");if(s.eventList?.length){const d=s.eventList.map(x=>x.date).sort();$("from").value=d[0];$("to").value=d.at(-1)}const inc=new Set(s.includedVariables||[]);$("variables").innerHTML=(s.columns||[]).map(c=>`<label class="inline"><input class="var" type="checkbox" value="${esc(c)}" ${inc.has(c)?"checked":""}>${esc(c)}</label>`).join("");const keys=["timestamp","bsp","twa","tws","awa","aws","vmg","vmg_pct","heel","drift","lat","lon","sog","cog","sail"];$("mapping").innerHTML=keys.map(k=>`<div><label>${k.toUpperCase()}</label><select class="map" data-key="${k}"><option value="">Not mapped</option>${(s.columns||[]).map(c=>`<option ${s.mapping?.[k]===c?"selected":""}>${esc(c)}</option>`).join("")}</select></div>`).join("");selectors()}
function selectors(){const s=NJ.state,nums=(s.includedVariables||[]).filter(c=>NJ.rows.some(r=>numeric(r[c]))),ts=s.mapping?.timestamp,axes=[...(ts&&s.includedVariables.includes(ts)?["__TIME__"]:[]),...nums];fill("gpsColour",nums,s.mapping?.vmg||s.mapping?.bsp);fill("varX",axes,s.mapping?.twa);fill("varY",axes,s.mapping?.bsp);fill("varColour",["None",...nums],s.mapping?.tws||"None");const winds=[...new Set((s.polar||[]).map(p=>p.tws))].sort((a,b)=>a-b);fill("polarTws",winds,winds[0])}
async function refresh(){try{const d=await post("/api/plot-data",request());NJ.rows=d.rows||[];delete d.rows;NJ.data=d;selectors();const note=d.plotDownsamplingApplied?`Figure downsampling active: ${d.returnedRows.toLocaleString()} of ${d.matchedRows.toLocaleString()} matched points shown.`:`Figures use all ${d.returnedRows.toLocaleString()} matched points.`;$("status").textContent=`${NJ.state.loadStatus} ${note}`;msg([...(d.warnings||[]),note].join(" "));render()}catch(e){msg(e.message)}}
function render(){const m=NJ.state.mapping,r=NJ.rows,metric=(n,v)=>`<div class="metric">${n}<strong>${esc(v)}</strong></div>`;$("metrics").innerHTML=metric("Stored rows",NJ.state.rowCount.toLocaleString())+metric("Matched rows",NJ.data.matchedRows.toLocaleString())+metric("Figure points",NJ.data.returnedRows.toLocaleString())+metric("Heap",`${NJ.state.heapUsedMiB} / ${NJ.state.heapMaxMiB} MiB`);const ts=m.timestamp,cols=[m.bsp,m.tws,m.vmg,m.vmg_pct,m.heel,m.twa].filter((x,i,a)=>x&&a.indexOf(x)===i&&NJ.state.includedVariables.includes(x));Plotly.react("overviewPlot",cols.map((c,i)=>({x:r.map(x=>x[ts]),y:r.map(x=>x[c]),type:"scatter",mode:"lines",name:c,line:{color:i?undefined:ORANGE}})),{...layout("Selected time series"),height:440,xaxis:{type:"date",title:"Time"}},{responsive:true});table(r.slice(0,500));renderActive()}
function purgePlot(id){const e=$(id);if(e?.data)Plotly.purge(e)}
function renderActive(){["polar","sails","gps","variable"].filter(x=>x!==NJ.active).forEach(x=>purgePlot(x+"Plot"));if(NJ.active==="polar")polar();else if(NJ.active==="sails")sails();else if(NJ.active==="gps")gps();else if(NJ.active==="variable")variable()}
function table(rows){const e=$("dataTable");if(!rows.length){e.innerHTML="<tr><td>No data</td></tr>";return}const cols=Object.keys(rows[0]);e.innerHTML=`<thead><tr>${cols.map(c=>`<th>${esc(c)}</th>`).join("")}</tr></thead><tbody>${rows.map(r=>`<tr>${cols.map(c=>`<td>${esc(numeric(r[c])?Number(r[c].toFixed(2)):r[c])}</td>`).join("")}</tr>`).join("")}</tbody>`}
function polar(){const m=NJ.state.mapping;if(!m.bsp||!m.twa)return;const target=Number($("polarTws").value),tol=Number($("polarTol").value)||1,r=NJ.rows.filter(x=>numeric(x[m.bsp])&&numeric(x[m.twa])&&(!m.tws||Math.abs(x[m.tws]-target)<=tol)),theta=r.map(x=>Math.abs(((x[m.twa]+180)%360+360)%360-180)),p=(NJ.state.polar||[]).filter(x=>x.tws===target);Plotly.react("polarPlot",[{theta,r:r.map(x=>x[m.bsp]),type:"scatterpolar",mode:"markers",name:"Actual",marker:{size:5,color:r.map(x=>x[m.tws]),colorscale:"Turbo"}},{theta:p.map(x=>x.twa),r:p.map(x=>x.targetBsp),type:"scatterpolar",mode:"lines",name:"Target",line:{color:ORANGE,width:4}}],{...layout("Boat speed and target polar"),polar:{sector:[0,180],angularaxis:{direction:"clockwise",rotation:90}}},{responsive:true})}
function sails(){const points=NJ.state.sails||[],groups=new Map();for(const p of points){if(!groups.has(p.sail))groups.set(p.sail,[]);groups.get(p.sail).push(p)}const traces=[];for(const [name,raw] of groups){const p=raw.slice().sort((a,b)=>a.order-b.order);if(p.length){p.push(p[0]);traces.push({theta:p.map(x=>x.twa),r:p.map(x=>x.tws),type:"scatterpolar",mode:"lines",fill:"toself",name,line:{color:p[0].colour,width:p[0].width},fillcolor:hexRgba(p[0].colour,p[0].opacity),hovertemplate:`${esc(name)}<br>TWA %{theta:.0f}°<br>TWS %{r:.1f} kn<extra></extra>`})}}if(!traces.length){msg("The sail chart loaded no sail boundaries.");return}Plotly.react("sailsPlot",traces,{...layout("Sail selection crossover chart"),polar:{sector:[0,180],angularaxis:{direction:"clockwise",rotation:90,title:"TWA"},radialaxis:{title:"TWS (kn)",angle:90}}},{responsive:true})}
function hexRgba(hex,a){const h=String(hex||"#ffffff").replace("#","");return `rgba(${parseInt(h.slice(0,2),16)},${parseInt(h.slice(2,4),16)},${parseInt(h.slice(4,6),16)},${Math.max(0,Math.min(1,Number(a)||0))})`}
function gps(){const m=NJ.state.mapping,c=$("gpsColour").value;if(!m.lat||!m.lon||!c)return;const r=NJ.rows.filter(x=>numeric(x[m.lat])&&numeric(x[m.lon]));Plotly.react("gpsPlot",[{lat:r.map(x=>x[m.lat]),lon:r.map(x=>x[m.lon]),type:"scattermap",mode:"markers+lines",marker:{size:7,color:r.map(x=>x[c]),colorscale:"Turbo",colorbar:{title:c}}}],{...layout(`GPS track by ${c}`),map:{style:"open-street-map"}},{responsive:true})}
const axis=(row,v)=>v==="__TIME__"?row[NJ.state.mapping.timestamp]:row[v];function variable(){let x=$("varX").value,y=$("varY").value,c=$("varColour").value,kind=$("varKind").value;if(!x||!y)return;const r=NJ.rows.filter(a=>axis(a,x)!=null&&axis(a,y)!=null),marker={size:6,opacity:.65,color:c!=="None"?r.map(a=>a[c]):ORANGE,colorscale:"Turbo",showscale:c!=="None"};const trace=kind==="Polar"?{theta:r.map(a=>a[x]),r:r.map(a=>a[y]),type:"scatterpolar",mode:"markers",marker}:{x:r.map(a=>axis(a,x)),y:r.map(a=>axis(a,y)),type:"scatter",mode:"markers",marker};Plotly.react("variablePlot",[trace],layout(`${y} against ${x}`),{responsive:true})}
async function save(){const mapping={};document.querySelectorAll(".map").forEach(e=>mapping[e.dataset.key]=e.value||null);NJ.state=await post("/api/settings",{includedVariables:[...document.querySelectorAll(".var:checked")].map(e=>e.value),mapping,plotDownsamplingEnabled:$("downsample").checked,maxPlotPoints:Number($("maxPoints").value)});configure();await refresh();msg("Settings saved.")}
async function exportCsv(){const r=await fetch("/api/export",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(request())});if(!r.ok)throw new Error("Export failed");const a=document.createElement("a");a.href=URL.createObjectURL(await r.blob());a.download="nightjar_filtered_log_0.9.1.csv";a.click();setTimeout(()=>URL.revokeObjectURL(a.href),5000)}
document.addEventListener("DOMContentLoaded",()=>{$("signIn").onclick=async()=>{try{await post("/api/login",{password:$("password").value});show(true);NJ.state=await api("/api/state");configure();await refresh()}catch(e){msg(e.message)}};$("logout").onclick=async()=>{await api("/api/logout",{method:"POST"});location.reload()};$("upload").onsubmit=async e=>{e.preventDefault();try{NJ.state=await api("/api/upload",{method:"POST",body:new FormData(e.target)});configure();await refresh()}catch(x){msg(x.message)}};$("apply").onclick=refresh;$("save").onclick=save;$("all").onclick=()=>document.querySelectorAll(".var").forEach(e=>e.checked=true);$("none").onclick=()=>document.querySelectorAll(".var").forEach(e=>e.checked=false);$("downsample").onchange=()=>$("maxPoints").disabled=!$("downsample").checked;$("csv").onclick=()=>exportCsv().catch(e=>msg(e.message));$("session").onclick=()=>location.href="/api/session";["polarTws","polarTol"].forEach(id=>$(id).onchange=polar);$("gpsColour").onchange=gps;["varX","varY","varColour","varKind"].forEach(id=>$(id).onchange=variable);$("tabs").onclick=e=>{const b=e.target.closest(".tab");if(!b)return;document.querySelectorAll(".tab,.page").forEach(x=>x.classList.remove("active"));b.classList.add("active");$("page-"+b.dataset.page).classList.add("active");NJ.active=b.dataset.page;renderActive();window.dispatchEvent(new Event("resize"))};boot().catch(e=>msg(e.message))});
